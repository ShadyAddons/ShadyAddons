package cheaters.get.banned.config.components;

import cheaters.get.banned.config.Setting;

public class CheckboxButton extends ConfigButton {

    public CheckboxButton(Setting setting, int x, int y) {
        super(setting, x, y);
    }

}
